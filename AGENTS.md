# AGENTS.md

## 项目定位

本项目用于构建一个通用的“北邮人论坛就业招聘信息整理工具”。工具通过北邮人论坛官方 Telnet 访客入口，只读归档就业相关版面的帖子，将论坛中难以筛选的非结构化内容转换为：

- 每帖一个、可长期保存和全文检索的 Markdown 文件；
- 可增量更新、可恢复、可查重的本地状态；
- 可全文检索和结构化筛选的 SQLite/JSON 查询接口；
- 可筛选和排序的 Excel 招聘信息索引。

项目核心是数据采集、归档、结构化和检索，不面向某一位具体求职者。不要把个人简历、毕业院校、目标城市、毕业时间、岗位偏好或个性化推荐规则写入核心抓取与索引逻辑。个性化筛选应作为归档数据之上的独立应用层实现。

仓库同时提供 Agent 查询层和 `skills/byr-job-advisor/`。Agent 查询层只能读取本地归档；就业建议必须区分原帖事实、自动提取字段和模型推断。

## 目标版面

当前支持三个就业相关版面：

| 版面代号 | 中文名称 | 主要内容 |
|---|---|---|
| `JobInfo` | 招聘信息专版 | 校招、社招、实习、内推等招聘帖子 |
| `Job` | 毕业生找工作 | 应届求职、就业经验和招聘讨论 |
| `Jump` | 跳槽就业 | 社招、跳槽和职业发展信息 |

新增版面时，应同时更新主程序中的版面映射、输出目录、Excel 汇总和文档。

## 核心需求

### 1. 只读采集

- 使用北邮人论坛官方 `guest` Telnet 入口，不要求用户提供论坛账号、密码或浏览器 Cookie。
- 默认主机为 `bbs.byr.cn:23`。
- 支持直连和 SOCKS5 代理。
- 正确处理 Telnet 协议控制字符、ANSI 终端控制序列和论坛使用的 GBK 编码。
- 不发帖、不回帖、不修改论坛内容。

### 2. 时间范围和目录扫描

- 默认首次归档最近 365 天的帖子。
- 支持选择一个或多个版面。
- 支持自定义归档天数、单版面最大帖子数和单帖最大翻页数。
- 目录时间只有月日时，需要跨自然年反向扫描并推断年份；正文页中的完整时间优先作为最终发布时间。

### 3. 正文归档

- 每篇帖子保存为独立 Markdown 文件。
- 文件名使用版面内稳定的 Telnet 文章编号，不使用可能变化的标题。
- Markdown 包含 YAML 元数据、结构化摘要和尽可能完整的原帖正文。
- 原始正文是最终事实依据；自动提取字段只用于检索和初筛。

默认目录结构：

```text
北邮人论坛近一年归档/
├── JobInfo-招聘信息专版/
│   ├── 39731.md
│   └── ...
├── Job-毕业生找工作/
├── Jump-跳槽就业/
├── state.json
├── byr_jobs.sqlite3
└── 北邮人论坛招聘索引.xlsx
```

### 4. 增量、查重和恢复

- 帖子唯一键为 `版面:文章编号`，例如 `JobInfo:39731`。
- 正文使用 SHA-256 哈希判断是否变化。
- 同一唯一键且内容未变化时，不重写 Markdown。
- 同一唯一键但内容发生变化时，更新原文件和索引，不创建副本。
- 首次回填被中断后，若 `state.json` 记录和 Markdown 文件均存在，续跑必须在本地快速跳过，不再次逐篇联网读取。
- 首次回填完成后，日常增量默认只复查每个版面最新 30 个编号，以捕获近期编辑。
- `--full-rescan` 明确要求重新联网检查时间范围内的全部帖子。
- 每成功处理一篇帖子都应原子更新 `state.json`。
- 每完成一个版面、连接失败或用户按 `Ctrl+C` 时，都应尽力根据当前状态更新 SQLite 和 Excel 派生索引。
- 不得因为远端帖子删除、连接失败或解析异常而删除已经归档的本地文件。

### 5. 结构化字段

当前自动提取并写入状态和 Excel 的字段包括：

- 是否招聘帖；
- 版面、文章编号、标题、作者和发布时间；
- 公司或机构；
- 校招、社招、实习、内推、兼职、全职等招聘类型；
- 产品、运营、算法、研发、数据、销售/市场等岗位类别；
- 工作地点；
- 毕业届别；
- 学历要求；
- 实习周期和到岗要求；
- 工作经验要求；
- 邮箱、微信、QQ、电话等联系方式；
- 投递或相关 URL；
- 正文摘要、正文字符数和抓取完整性；
- Markdown 相对路径、首次归档时间、最后检查时间和内容哈希。

字段提取当前基于正则表达式和关键词表，必须允许空值。不要为了提高表面完整率而猜测帖子中没有明确表达的信息。

### 6. Excel 输出

工作簿由 `state.json` 重建，当前包含：

- `汇总`：帖子总数、招聘帖数量和各版面数量；
- `招聘帖速览`：仅保留识别为招聘信息的记录和常用字段；
- `帖子索引`：所有归档帖子的完整结构化索引。

Excel 应具备筛选表、冻结表头、合理列宽、日期和数字格式，并通过公式错误扫描和可视化检查。Excel 是派生物；`state.json` 和 Markdown 才是可恢复的数据来源。

### 7. Agent 查询输出

- `byr_jobs.sqlite3` 由 `state.json` 和 Markdown 原文原子重建，不得成为新的唯一事实来源。
- `byr_job_query.py` 默认输出 UTF-8 JSON，并提供 `stats`、`search`、`get`、`export-jsonl`、`index` 和 `serve`。
- `search` 默认只返回招聘帖，支持全文、版面、地点、类型、岗位、届别、机构和时间范围筛选。
- HTTP API 只允许 GET，默认绑定 `127.0.0.1`，不得在没有独立认证设计时暴露到公网。
- 每个结果必须保留稳定键、Markdown 路径、抓取完整性、内容哈希和发布时间，便于 Agent 核查。
- SQLite 中的正文用于搜索与局部读取；批量回答不得无边界返回所有正文或联系方式。

## 已完成的工具

### 主流程

#### `byr_job_archive.py`

当前唯一推荐的归档入口，已经实现：

- 三个就业版面的目录反向扫描；
- Telnet `guest` 自动登录；
- GBK 解码、ANSI 清理和多屏正文合并；
- 直连及 SOCKS5 代理；
- 最近若干天回填和近期编号增量复查；
- 中断续跑、本地快速跳过和 SHA-256 查重；
- 单帖 Markdown 写入；
- `state.json` 原子更新；
- 招聘字段自动提取；
- 连接超时、访客席位已满、正文读取失败和 `Ctrl+C` 处理；
- 版面完成或异常退出时的 SQLite/Excel 检查点更新。

常用命令：

```bash
# 通过本机 SOCKS5 代理抓取默认三个版面
python3 byr_job_archive.py --proxy 127.0.0.1:7890

# 当前网络允许访问 23 端口时直连
python3 byr_job_archive.py

# 只抓指定版面
python3 byr_job_archive.py \
  --boards JobInfo Jump \
  --proxy 127.0.0.1:7890

# 只根据已有状态重建 Excel，不访问论坛
python3 byr_job_archive.py --rebuild-excel

# 小范围连通性和解析测试
python3 byr_job_archive.py \
  --max-posts 3 \
  --proxy 127.0.0.1:7890

# 强制复查时间范围内全部帖子
python3 byr_job_archive.py \
  --full-rescan \
  --proxy 127.0.0.1:7890
```

运行环境至少需要 Python 3.10 和 `pexpect`。如果系统默认 Python 缺少依赖，应使用已经安装 `pexpect` 的虚拟环境或 Conda 环境，不要修改脚本来绕过导入错误。

#### `build_byr_job_index.mjs`

Excel 构建器，使用 `@oai/artifact-tool`：

- 读取 `state.json`；
- 按发布时间和文章编号倒序排列；
- 生成三个工作表、筛选表和汇总公式；
- 保留中国时区的墙上时间，避免 Excel 日期发生八小时偏移；
- 支持输出有限范围预览；
- 检查关键区域和常见公式错误。

主程序会调用该脚本。除调试 Excel 外，不应要求用户直接运行它。

#### `byr_job_store.py` 与 `byr_job_query.py`

Agent 查询层：

- 从 `state.json` 和 Markdown 构建 `byr_jobs.sqlite3`；
- 用 `posts` 保存结构化字段与正文，用 `post_facets` 保存多值筛选维度；
- 优先使用 SQLite FTS5 trigram 分词支持中文子串检索；
- 提供稳定 JSON CLI、JSONL 导出和只读 HTTP API；
- 状态更新后首次查询可自动重建数据库；
- 数据库写入使用同目录临时文件和原子替换，失败时保留旧数据库。

常用命令：

```bash
python3 byr_job_query.py stats
python3 byr_job_query.py search --query "算法" --location 北京 --limit 20
python3 byr_job_query.py get JobInfo:39728
python3 byr_job_query.py serve
```

#### `skills/byr-job-advisor/`

面向 Codex/Agent 的可安装 Skill。它要求先用 JSON 接口筛选，再读取少量原帖；回答时必须说明归档覆盖时间，并把事实、提取提示和建议分开。

#### `byr_telnet_utf8.py`

交互式 UTF-8 终端客户端。它负责 Telnet 协商并在本地 UTF-8 终端和论坛 GBK 编码之间转换，适合人工验证论坛连接、按键和页面内容。

#### `北邮人论坛归档工具说明.md`

面向使用者的运行说明。任何 CLI、默认值、目录结构、异常行为或依赖变化，都应同步更新该文件和本文件。

## 数据流

```text
Telnet guest
    ↓
版面目录扫描与日期推断
    ↓
文章多屏读取、GBK 解码与终端噪声清理
    ↓
正文解析与字段提取
    ↓
SHA-256 查重
    ├── 每帖 Markdown
    └── state.json
              ↓
      ├── SQLite / JSON / HTTP
      └── Excel 索引构建器
```

## 状态文件约定

`state.json` 是增量运行的核心，不得随意重置。主要顶层字段：

- `schema_version`：状态格式版本；
- `created_at`、`updated_at`：状态创建和更新时间；
- `boards`：各版面最高编号、扫描时间、截止日期和首次回填完成标记；
- `posts`：以 `版面:文章编号` 为键的帖子元数据。

更改状态结构时必须：

1. 保持旧状态可读取，或提供明确迁移逻辑；
2. 增加 `schema_version`；
3. 确保中途退出不会留下半写 JSON；
4. 用真实或精简状态验证 Excel 仍可重建。

## 开发约束

- `byr_job_archive.py` 是抓取、解析和状态行为的来源真相。
- 不要将某个用户的求职偏好加入关键词分类、招聘帖判定或 Excel 固定列。
- 不读取或复制用户的浏览器 Cookie、论坛密码或其他私人凭据。
- 默认保持访客只读访问；任何需要登录账号或对论坛产生写操作的设计都必须作为独立能力讨论。
- 不要使用标题作为文件标识，也不要改变现有 `版面:文章编号` 唯一键。
- 不要在网络错误时清空状态、覆盖已有正文或把一次不完整抓取标记为完整。
- 保留 GBK/Telnet 边界；本地文件、JSON、Markdown 和源码统一使用 UTF-8。
- 修改关键词或正则时，应同时测试误报和漏报，不只测试命中样例。
- 新增结构化字段时，同步修改：解析结果、状态记录、Markdown 展示、Excel 列和使用文档。
- `README.md` 面向普通用户，只重点说明工具功能、生成 Excel 和安装 Agent Skill 两条使用路径；不要在首页展开 SQLite、JSON、HTTP、表结构或内部验证流程，相关技术细节放入开发说明和 Skill references。
- 输出目录属于运行数据。除非任务明确要求，不要批量改写、移动或删除已有归档。
- `node_modules` 可能是指向受管运行时的符号链接；不要把依赖目录复制进项目或提交运行时文件。

## 当前已知限制

- Telnet 23 端口在部分网络不可达，可能必须使用 SOCKS5 代理。
- 访客席位可能已满，连接会被论坛暂时拒绝。
- 论坛是终端界面，页面提示、分页布局或 ANSI 行为变化可能破坏解析。
- 初次回填需要逐篇读取正文，速度主要受论坛响应和文章页数限制。
- 自动分类和字段提取是启发式规则，不能代替人工查看原文。
- Web 文章 URL 与 Telnet 文章编号并非始终可直接互换，目前 Markdown 中的稳定来源标识是 Telnet 版面和文章编号。
- 抓取与解析仍缺少完整的自动化测试套件；Agent 查询层已有离线单元测试，联网行为仍依赖小范围 Telnet 冒烟测试。

## 修改后的最低验证要求

### Python 或抓取逻辑

```bash
python3 -m py_compile byr_job_archive.py
python3 byr_job_archive.py --help
```

若当前 Python 没有 `pexpect`，第二条命令应改用项目实际运行的 Python 环境。

涉及联网逻辑时，先做小范围测试：

```bash
python3 byr_job_archive.py \
  --boards JobInfo \
  --max-posts 3 \
  --skip-excel \
  --proxy 127.0.0.1:7890
```

确认：

- 不会重复写入相同帖子；
- 中断后能从本地快速跳过；
- `state.json` 始终是有效 JSON；
- 缺失 Markdown 的状态记录会触发重新抓取；
- 网络错误不会损坏已有归档。

### Excel

```bash
python3 byr_job_archive.py --rebuild-excel
```

确认：

- 工作簿可以打开；
- 三个工作表存在；
- 汇总数量与 `state.json` 一致；
- 表头、筛选、冻结窗格、日期和长文本可读；
- 没有 `#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?` 或 `#N/A`；
- 不因预览完整数千行而产生超大渲染或检查输出。

### Agent 查询层

```bash
python3 -m py_compile byr_job_store.py byr_job_query.py
python3 -m unittest discover -s tests -v
python3 byr_job_query.py --help
python3 byr_job_query.py index
python3 byr_job_query.py stats
```

确认：

- SQLite `PRAGMA integrity_check` 为 `ok`；
- 帖子总数与 `state.json` 一致；
- 中文正文关键词可以命中；
- 地点、招聘类型和岗位类别多值筛选正确；
- `get` 返回原帖正文、稳定键和来源元数据；
- `state.json` 更新后查询库可以自动重建；
- HTTP API 没有写入端点。

## 建议的后续建设

优先级从高到低：

1. 为目录年份推断、屏幕合并、正文解析、字段提取和续跑跳过增加离线单元测试；
2. 将硬编码关键词、机构和地点表迁移到可维护配置；
3. 合并或淘汰早期 Telnet 辅助脚本，减少重复连接实现；
4. 增加结构化日志、连接重试和失败帖子清单；
5. 改善可点击 Web 原帖链接的可靠解析，同时保留 Telnet 稳定标识；
6. 将通用归档层与个性化搜索、匹配、推荐层明确拆分；
7. 评估标准 MCP 适配器或远程受控 API；本地 JSON CLI/HTTP 已覆盖基础 Agent 接入。

## 完成标准

一个改动只有在以下条件满足后才算完成：

- 不破坏只读、通用和可恢复的项目边界；
- 现有 `state.json` 与 Markdown 仍然兼容；
- 增量运行不会无故重新读取全部历史帖子；
- 中断和网络异常不会丢失已完成工作；
- Excel 可以由状态独立重建；
- 用户文档、CLI 帮助和本文件与实际行为一致；
- 已执行与改动风险相称的语法检查、离线测试、联网冒烟测试或 Excel 验证。
