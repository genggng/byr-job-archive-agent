# 北邮人论坛就业版面增量归档工具

这个工具通过北邮人论坛官方 Telnet `guest` 只读入口归档以下版面：

- `JobInfo`：招聘信息专版
- `Job`：毕业生找工作
- `Jump`：跳槽就业

默认首次下载最近 90 天的全部帖子。每篇帖子保存为单独的 Markdown 文件，同时生成面向 Agent 的 SQLite 查询库和一个可以筛选的 Excel 索引。再次运行时只扫描最新编号附近的帖子；相同内容不会重复写入。

## 文件

- `byr_job_archive.py`：主程序，负责目录扫描、正文抓取、去重和 Markdown 保存
- `byr_job_store.py`：根据状态和 Markdown 原文构建可恢复的 SQLite 查询库
- `byr_job_query.py`：输出 JSON 的查询 CLI、JSONL 导出和只读 HTTP API
- `build_byr_job_index.mjs`：根据归档状态生成 Excel
- `北邮人论坛招聘信息归档/`：默认输出目录，首次运行时自动创建

默认目录结构：

```text
北邮人论坛招聘信息归档/
├── JobInfo-招聘信息专版/
│   ├── 39728.md
│   └── ...
├── Job-毕业生找工作/
├── Jump-跳槽就业/
├── state.json
├── byr_jobs.sqlite3
└── 北邮人论坛招聘索引.xlsx
```

`state.json` 是增量抓取和去重依据，请不要随意删除。单篇 Markdown 使用稳定的“版面 + Telnet 文章编号”标识，因此标题修改也不会产生第二份文件。SQLite 和 Excel 都是派生物，可以随时从 `state.json` 和 Markdown 重建。

## 运行

先进入工具目录：

```bash
git clone https://github.com/genggng/byr-job-archive-agent.git
cd byr-job-archive-agent
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
```

```bash
python3 byr_job_archive.py
```

首次运行需要从最新目录一直翻到 90 天前，并逐篇读取正文，因此可能持续较长时间。程序会每处理完一篇就更新 `state.json`；每完成一个版面会生成一次 SQLite 和 Excel 检查点，连接失败或中途按 `Ctrl+C` 退出时也会尝试根据当前状态更新派生索引。重新执行相同命令即可继续。续跑时，`state.json` 中已有记录且 Markdown 文件仍存在的帖子会在本地直接跳过，不再连接论坛逐篇读取。

## Agent 查询

必须先运行归档脚本，成功建立本地数据库后再查询：

```bash
python3 byr_job_archive.py
```

如果已有 `state.json` 和 Markdown、但数据库尚未建立或已经过期，可以先手动重建：

```bash
python3 byr_job_query.py index
```

查看归档覆盖范围和可用筛选值：

```bash
python3 byr_job_query.py stats
```

检索招聘帖（默认输出 JSON，默认排除讨论帖）：

```bash
python3 byr_job_query.py search \
  --query "大模型" \
  --location 北京 \
  --type 实习 \
  --since 2026-06-01 \
  --limit 20
```

根据稳定唯一键读取完整原帖：

```bash
python3 byr_job_query.py get JobInfo:39728
```

启动仅供本机使用的只读 HTTP API：

```bash
python3 byr_job_query.py serve --host 127.0.0.1 --port 8765
```

可访问 `/health`、`/v1/stats`、`/v1/posts` 和 `/v1/posts/{唯一键}`。该服务没有身份验证，不应直接绑定公网地址。

导出含原帖正文的 JSONL：

```bash
python3 byr_job_query.py export-jsonl --output byr-jobs.jsonl
```

## 常用选项

只抓某些版面：

```bash
python3 byr_job_archive.py \
  --boards JobInfo Jump
```

首次只抓最近 180 天：

```bash
python3 byr_job_archive.py \
  --days 180
```

完整复查所选时间范围。正文没有变化的文件仍不会重写：

```bash
python3 byr_job_archive.py --full-rescan
```

只根据现有 `state.json` 重新生成 Excel，不连接论坛：

```bash
python3 byr_job_archive.py --rebuild-excel
```

只根据现有 `state.json` 和 Markdown 重建 SQLite，不连接论坛：

```bash
python3 byr_job_archive.py --rebuild-agent-index
```

小范围测试，每个版面只检查 3 篇：

```bash
python3 byr_job_archive.py --max-posts 3
```

查看全部参数：

```bash
python3 byr_job_archive.py --help
```

## 增量和查重逻辑

1. 唯一键为 `版面:Telnet文章编号`，例如 `JobInfo:39728`。
2. 每篇正文计算 SHA-256 内容哈希。
3. 首次回填中断后续跑：已有状态记录且 Markdown 文件存在的帖子会在本地快速跳过，不进行 Telnet 正文读取。
4. 正常增量复查中，已有唯一键且哈希相同：记为“联网确认重复”，不重写 Markdown。
5. 已有唯一键但哈希变化：更新同一个 Markdown，并在 Excel 中刷新字段。
6. 默认每次复查各版面最后 30 个编号，用来发现发帖人对近期帖子的编辑。
7. 超过复查窗口的旧帖不再逐篇打开，因此日常增量运行速度较快。
8. 使用 `--full-rescan` 可以重新检查所选时间范围内的全部帖子；该模式不会使用本地快速跳过。

可以通过 `--rescan-last 100` 扩大近期复查窗口：

```bash
python3 byr_job_archive.py --rescan-last 100
```

## Excel 内容

工作簿包含三个工作表：

- `汇总`：帖子数量和版面数量
- `招聘帖速览`：只展示自动识别为招聘信息的帖子和关键字段
- `帖子索引`：三个版面的全部归档帖子

自动提取字段包括：

- 是否招聘帖
- 公司或机构
- 校招 / 社招 / 实习 / 内推
- 产品 / 运营 / 算法 / 研发 / 数据等岗位类别
- 地点、毕业届别、学历、实习周期、经验要求
- 邮箱、微信 / QQ / 电话和投递链接
- Markdown 路径、正文摘要、归档时间和内容哈希

字段使用规则匹配自动提取，方便初筛，但可能出现遗漏或误判。重要岗位请以对应 Markdown 中的原帖正文为准。

## 异常处理

### 访客席位已满

稍等几分钟后重新运行。已完成的文章不会重复保存。

### 连接失败

检查当前网络和论坛状态，稍后重新运行。已经归档的内容不会丢失。

### 只想保留 Markdown，暂时不生成 Excel

```bash
python3 byr_job_archive.py \
  --skip-excel
```

之后可用 `--rebuild-excel` 补生成 Excel。

如本次也不希望更新 SQLite，可同时添加 `--skip-agent-index`。之后执行 `python3 byr_job_query.py index` 即可补建。

### 个别文章抓取失败

程序会记录失败并重连，再次执行相同命令即可重试。论坛删除的文章可能无法恢复，已经成功归档的旧文件不会因此被删除。
